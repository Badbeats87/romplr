# Third-Party Dependencies

This project uses Git submodules for the following:

- **circle-stdlib** — Bare-metal Raspberry Pi framework with C++ standard library
- **CMSIS_5** — ARM Cortex Microcontroller Software Interface Standard
- **buildroot** — Linux distribution builder

Initialize submodules with:
```bash
git submodule update --init --recursive
```
